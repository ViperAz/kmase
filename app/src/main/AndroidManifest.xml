<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sliepnier.projectse" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Loading_Activity"
            android:label="SEProject" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DisplayMessageActivity"
            android:label="@string/title_activity_display_message"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.sliepnier.projectse.MainActivity" />
        </activity>
        <!-- <activity android:name=".Main_menu" > -->
        <!-- </activity> -->
        <!-- <activity android:name=".Loading_Activity" > -->
        <!-- </activity> -->
        <activity android:name=".setA" >
        </activity>
        <activity android:name=".SetFoodLV" >
        </activity>
    </application>

</manifest>
